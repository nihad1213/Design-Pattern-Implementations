# Singleton

Singleton is Creational Design Pattern. It ensures that class has only one instance while providing a global point of access to that single instance. That prevents accidental creation of multiple instances and ensures controlled, eficient use of resources like memory and connections. 

Singleton is used in:
Database Connection
Logger
Cache Manager